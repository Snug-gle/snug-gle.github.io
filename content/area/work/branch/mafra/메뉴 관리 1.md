---
created: 2025-07-21
---
## 요구사항 
- 메뉴 관리 UI를 AUI Grid로 변경

## 스케치
- 메뉴, 메타파일 엔티티 연관 관계 확인 필요
- 기존 이미지 처리 flow
	1. img.src 변수에 업로드 이미지 result를 할당
		- data:image/svg+xvl;base64,PD94bw......
	2. iconImageFile이 없으면 경로에 있는 파일 할당
		- 아이피:포트/src/assets/default-icon-set/CHATTING.svg
	3. iconImageFile이 있고, iconImageFile의 url이 있으면 api 요청 한 후 결과를 img.src에 넣음
	4. 이미지 없는 경우
		- 아이피:포트/view/undefined
- 버튼 라벨 텍스트 처리
	1. 
- 저장된 이미지 파일 렌더 구현
	- row.iconFile 선언
	- 

- 기본 이미지 파일 렌더 구현

- 업로드 이미지 파일 렌더 구현
	- row.iconFile 선언
	- row.iconSrc에 업로드 이미지 base64를 대입 / quick의 경우 quickIconSrc
	- row 업데이트
	- row 재로드

- 이미지 초기화 버튼 구현
	- 기존
		- row에  iconImageFile이 있는 경우, 그 downloadUrl을
		- row에 quckIconImageFile이 있는 경우 그 downloadUrl을
		- row에 children이 있는 경우 children을 재귀로 이 과정 다시 실행
		- 위 url을 담은 변수(즉, 아이콘이 없으면) return
		- 있으면
			- REMOVE_ICON_BASE
				- fileMetaService에서 deleteByToken
				- menu.setIconImageFile(null)
			- REMOVE_ICON_QUICK
				- fileMetaService에서 deleteByToken
				- menu.setQuickIconImageFile(null)
- 이미지 저장 버튼 구현
	- 유효성 체크
	- 수정된 내용 있는지 없는 지 체크
	- 데이터 그대로 업데이트
	- 파일이 있기 때문에 multiPartForm 사용  new FormData