---
created: 2025-06-23
---
### 요구사항
---
- AS - IS
	- 전화 단일 채널
	- 콜백 티켓 최초 생성 -> 담당자 배정 후 담당자가 업무 진행 시 새로운 티켓이 생성(아웃바운드 티켓이 생성된다)
	- 처리 내역이 적은 상담사 에게 배정
- To - Be
	- [x] 전화 외 챗봇과 같은 채널이 추가
	- [-] 담당자 지정 시 티켓이 생성되지 않게 하기
	- [-] 순차 배정
	- 자동 분배 설정 AUI Grid로 변경

### 소스 확인
---
- "CALLBACK_TICKET_EID", - 상속 - BaseAuditingUserIdSequenceAbstractEntity(Entity_id)
- "CREATE_DATE", 상상속 - BaseAuditingTimeEntity
- "ENTITY_STATUS", 상상속 - BaseAuditingTimeEntity
- "MODIFIED_DATE", 상상속 - BaseAuditingTimeEntity
- "CREATE_BY",  - 상속 - BaseAuditingUserIdSequenceAbstractEntity
- "MODIFIED_BY",  - 상속 - BaseAuditingUserIdSequenceAbstractEntity
- "CALLBACK_NUMBER", - 내부 - CallbackTicket
- "INBOUND_PATH_CODE", - 내부 - CallbackTicket
- "RECEPTION_NUMBER", - 내부 - CallbackTicket
- "REPRESENT_NUMBER", - 내부 - CallbackTicket
- "REPRESENT_NUMBER_NAME", - 내부 - CallbackTicket
- "TICKET_EID", - manyToOne Ticket
- "CALLBACK_MANAGER",- 내부 - CallbackTicket
- "CALLBACK_STATUS"  - 내부 - CallbackTicket

- 테이블 이미지
	- [[Pasted image 20250414140808.png]]
	- [[Pasted image 20250414140808.png]]
	- [[Pasted image 20250414141739.png]]

- 다채널 확인
	- 서버
		-  관련 엔티티: 티켓(channelTypeCode), 티켓 채널(typeCode)
	- 화면
		- 인바운드시 값(IVR)이 inbountpath로 저장되는 로직 먼저 확인해보자
			- handleCallIn
				- save 시, 티켓 채널에 VOICE 
				- CounselFlowHubFactory
				- CounselFlowHubChannel
					- CounselFlowHubChatChannel
					- CounselFlowHubMailChannel
					- CounselFlowHubVoiceChannel
				- CounselFlowHubVoiceChannelFactory
				- CounselFlowHubChatChannelFactory
				- CounselFlowHubMailChannelFactory
					- import CounselFlowHubFactory
- 자동분배 설정 AUI Grid로 변경
	-  현재 
	![[Pasted image 20250516100837.png]]

![[Pasted image 20250516102217.png]]

		
### 공유 및 문의 사항
1. 콜백 엔티티 관련 문의
	- 콜백 건 처리 시 생성되는 두 개의 티켓 중 제거해야 할 티켓 특정
		- 담당자 지정 시 생성 -> 콜백 건 처리
		- 전화 걸어 active 시 생성 -> 생성 되지 않게 로직 수정
	-  콜백 건 처리를 위해 전화를 걸 때 아래의 두 개 버튼 모두 콜백 티켓 처리가 가능하게 해야 하는지
		- 상담 화면 하단 콜백 티켓 리스트(entityId 를 가져올 수 있지 않을까 생각됨)
		- 상담 화면 상단 걸기 버튼(entityId를 어떻게 가져올지 고민해야 하는 상태)
	- 알림
		- 번호는 제외
		- 티켓 -> 콜백 티켓
	- 콜백 티켓 엔티티
		- 처리 일시 추가 -> 처리 완료 시점 생각해둘 것
		- 아웃바운드 액티브 이후 정상 종료 시점 확인해보기
	- 화면
		- 티켓 담당자 -> 콜백 티켓 담당자..
		- 상담 하단 콜백 티켓 리스트 화면
			- Ticket_tm -> callback_tickek
	- 콜백번호가 Null 인 경우 수신번호 넣어주는 로직 확인
	- 메인 - 상담
		- 콜백 리스트에 전화번호는 콜백번호
		- 컬럼명 수정 필요
2.  다채널 관련 문의
	- inboundpath를 CTI 서버에서 내려준다면, 어느 포인트에서 내려줄까?
		- 로그인 시? 콜 수신 시? 상태 갱신 시?
	- 대표서비스
- 콜백 다채널 환경 검토
	- 기존 다채널 로직 확인
	- 자동분배 화면 AUIGrid로 변경
	- 하나의 그리드로 만들어 보니 CRUD 관련 UX에 혼선이 있을 수 있어 두 개의 그리드로 표현 진행
### 할 일 
- 아웃바운드 티켓 생성 및 통화 종료 시 콜백 리스트 상태 업데이트
	- [x] completeCallbackTicket -> updateCallback() 추가
		1. 상태값(callbackStatus) 변경: 미처리 -> 완료
		2. 처리 완료일(endDate) 인입
	- [x] completeCallbackTicket -> updateTicket() 기존 로직 확인
	- [x] 콜백번호가 null 일 시, 콜백번호 값에 수신번호가 들어가는 로직 확인하기
	- [x] 백 관리 조회 버튼 수정
		1. 티켓 상태 값 -> 콜백 상태 값
		2. 담당자
	- [x] 자동 담당자 지정 시 콜백 티켓 타입의 티켓 생성 되는 듯 ?
	- [x] admin 계정에서 다른 담당자에게 지정된 건까지 보임
	- [ ] 처리완료 처리하면 화면에서 상담 메인 화면 콜백리스트 화면에서 사라져야?
- 자동 분배 로직 수정
	- [x] 현재 로직은 callbackTicket entity에서 ticket eid가 없는 경우를 할당되지 않은 콜백으로 보고 있음 -> ticket을 predicate에 추가하는 로직을 확인하여 수정
	- [x] 현재 분배하는 로직 분석
	- [ ] 추후 로그인 시, 본인 근무 여부 false 이면 true로 업데이트
	- [ ] 근무 여부 true(기본) / false / 자동 분배시 체크 , 순차 분배다

### 진행 기록
- #### 콜백 테이블 엔티티 변경
	- [ ] new Ticket() -> CallBackEntity 필드 값(담당자, 처리 상태) 수정
	- [ ] status 필드
		1. 상위 코드 : CALLBACK_PROCESS_STATUS -> 상위코드 개념은 티켓의 경우에만 사용하는 것으로 보임.. 
		2. 하위 코드 :
			 - 미처리 : CALLBACK_UNPROCESSED
			 - 처리중 : CALLBACK_IN_PROCESS 
			 - 처리완료. : CALLBACK_COMPLETED
	- [ ] 알림 발송 타겟 변경: new Ticket()의 담당자 -> CallBackEntity의 담당자
	- 아웃바운드 시, CallBackEntity 완료처리
	- 수동 분배 요청 시
	- entity id, CallbackTicketSearchParameter(managerEntityId)를 넘겨줌 ->
		CallbackTicketSearchParameter에 entityId 가 들어감
	- 티켓 생성 로직을 완전히 제거했을 때 발생할 문제는 ? 
#### 자동 분배 시나리오 변경
- 콜백티켓 분배 스케줄러(5분)
	- autoDistributeCallback()
		- findUnassignedCallbackTickets() -> foundCallbacks
			- 콜백 상태가 null(담당자 미지정 상태)을 조회
		- groupCallbacksByRepresentNumber(foundCallbacks)
			-> callbacksByRepresentNumberMap
			- computeIfAbsent로 대표번호를 key로, 연관 값을 value로 변환
			 ** 대표번호가 없는 경우 리스트를 생성
			- add변경된 키-맵을 맵 객체에 넣음
		- foundCallbackTargetGroups(callbacksByRepresentNumberMap.keySet())
			-> 위 value를 그냥 리스팅 한거 같은데... 
#### 다채널
- codeType: "SYSTEM"
- code: "CHANNEL_TYPE"
- name: "채널 종류"
- orderNumber: 8
- children:
	- code: "VOICE"
	- name: "보이스"
	- orderNumber: 1
	- code: "CHAT"
	- name: "채팅"
	- orderNumber: 2
	- code: "E_MAIL"
	- name: "이메일"
	- orderNumber: 3
#### 엑셀 다운로드
- 콜백 관리 화면 샘플
- ![[Pasted image 20250530111031.png]]

### 테스트
1. 자동분배
	-  콜백 번호가 모두 다른 콜백 건 순차 분배
	- 일부 번호가 같은 콜백 건 순차 분배
	- 
--- 
## 메모
- channel state
	- ChannelState: {
	        NOTLOGIN: "NOTLOGIN",       // 로그인이 아닌 상태
	        READY: "READY",             // 대기 중
	        ABSENCE: "ABSENCE",         // 이석
	        PROCESS: "PROCESS",         // 작업(후처리)
	        RINGBACK: "RINGBACK",       // 전화 거는 중 kebhana01
	        OFFERING: "OFFERING",       // 전화 오는 중
	        BUSY: "BUSY",               // 수화기를 든 상태
	        ACTIVE: "ACTIVE",           // 통화 중
	        HELD: "HELD"                // 통화 보류
	},

- CounselFlowHubChannel
	- CounselFlowHubVoiceChannel
		- CounselFlowHubVoiceChannelIpron
			- ConselFlowHubFactory.create 
			- -> new ConselFlowHub(channels, eventCallbacks)
				- CounselFlowHubChatChannelFactory.create
					- (CounselFlowHubVoiceChannelIpron)
- 
- outbound시 나가는 데이터 모습은?
	- CounselFlowHubChannelInfo(id, password, extension, state("NOTLOGIN"), subState(null))
		- ConselFlowHubVoiceChannelInfo(state("NOTLOGIN"), subState(null), callInfo(null), callInfos([]))
	- CounselFlowHubChannel(options, channelType, channelInfo, enventCallbacks, connected, absenceReasonCodes, queueReportInfo)	
		- CounselFlowHubVoiceChannel(CounselFlowHubChannel, channelInfo(new CounselFlowHubVoiceChannelInfo))

- Status
	- NOTLOGIN
	- READY
	- ABSENCE
	- OFFERING
		- 전화 왔을 때
	- ACTIVE
	- PROCESS
		- 후처리
	- HELD
		- 보류
	- RINGBACK
		- 전화 거는 중
	- MISS
		- 호놓침
- ## 이슈 사항
	- 콜백 할당 시 메세지가 자동 / 수동 각각 다름
		- ? 자동 / 수동 을 문구에 넣어주면 어떨까?
	- 수동 할당의 경우, notification의 sender 필드에 값이 들어가나, 자동 할당의 경우 sender 필드 null 임
		- ? 기존 콜백 관리의 경우 sender.name (작성자) 로 data가 들어 갔었음.